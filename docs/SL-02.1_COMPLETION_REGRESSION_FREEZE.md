# SL-02.1 – Completion / Regression / Freeze Gate

Status: **FROZEN / PASS**
Date: 2026-09-16

## Frozen implementation authority

- Repository: `DrHoschi/DevForge`
- Implementation branch: `feature/sl-01-safe-frame-manipulation-foundation`
- Frozen implementation commit: `cf9cc44b573ec28ba5d61b76a2a3eb75d2dd411a`
- Commit message: `Implement SL-02.1 unified frame edit tool mode`
- Runtime build label: `SPRITE LAB · SL-02.1 · TESTBUILD 1`
- Frozen code surface: `tools/sprite-lab/index.html`
- Immutable reference branch: `freeze/sl-02.1-safe-frame-manipulation`

The freeze branch points exactly to the implementation commit above. This documentation commit is not part of the frozen runtime authority.

## Regression / completion result

The delivered SL-02.1 workflow remains intact across the established Sprite Lab path:

`PNG / atlas JSON import → frame selection → direct frame editing → multi-selection / batch editing → atlas export`

Confirmed capabilities at the frozen implementation include:

- existing atlas PNG and optional JSON import
- deterministic Sprite-Sheet construction from individual PNGs
- safe `Auswählen` navigation mode
- explicit `Frame bearbeiten` mode for move / corner resize
- `Frame zeichnen`
- `Anchor setzen`
- numeric frame properties: X, Y, W, H, Anchor X/Y, Scale
- single and multi-frame selection with Primary Frame authority
- batch width / height / size / X / Y operations from Primary Frame
- workspace zoom, fit view and 100 %
- JSON, PNG and combined PNG + JSON export
- responsive phone/tablet/desktop layout retained

## Device evidence

### iPhone

**PASS**

The SL-02.1 TESTBUILD 1 mobile workflow and explicit edit-mode separation were confirmed on the real device before this gate.

### iPad

**PASS – 2026-09-16**

Real-device evidence confirmed:

- files view and PNG/JSON atlas loading
- loaded construction atlas with 79 imported frames
- stable canvas / atlas presentation
- Files / Frames / Export tablet navigation
- single selection and Primary Frame
- multi-selection and batch controls
- interaction-mode selector exposing `Auswählen`, `Frame bearbeiten`, `Frame zeichnen`, `Anchor setzen`
- frame properties visible and coherent with the selected Primary Frame
- zoom / fit / 100 % controls
- export panel and JSON preview
- no blocking tablet layout regression visible in the completion evidence

## CI / automated status

No GitHub commit status checks are registered for the frozen implementation commit. Therefore this freeze does **not** claim an automated-CI PASS. Its regression authority is the reviewed code state plus the completed real-device gates above.

## Freeze boundary

SL-02.1 is frozen at the implementation commit stated above. No additional capability is included in this freeze.

In particular, **Undo / Redo is not part of SL-02.1**.

Any later change to the frozen runtime must be performed in a separately authorized follow-up block and must not silently mutate this authority.

## Authorized next block

The next permitted Sprite Lab block is:

**SL-02.2 – Undo / Redo History – Definition / Scope Reconciliation**

That next block is definition/reconciliation only. No Undo/Redo implementation is authorized by this freeze document.
